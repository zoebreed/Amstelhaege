from random import randrange
from math import floor, ceil
from code.classes.Water import Water

class randomWater:
    """
    places the bodies of water in a random position
    """
    def __init__(self, amstelhaege):
        self.amstelhaege = amstelhaege
        self.bodies = randrange(0, 5)
        self.total_area = 5760

        self.max_width = 
        self.water_width = randrange(4, 150)
        self.water_length = randrange(int(ceil(water_area / water_width)), water_width)

    
    def run():
        index_water
        water_area = perecentages[index_water] * water.water_area

        # dimension of water
        min_length = max(1, int(sqrt(water_area)
        max_length =
        water_length = random.randint(min_length, max_length)

        # ... coordinates
        x = random.randint()
        y = random.randint()
        # set coordinates
        water.x_bottom_left = x
        water.y_bottom_left = y
     


        for i in range(self.bodies - 1):
            area = floor(self.remaining_area/randrange(self.remaining_area/4))
            self.remaining_area -= area
            areas.append(area)
        area.append(self.remaining_area)

        # ab + cd + ef + gh = 5760
        # where minimally 1:4
